# Доменные модели

## Puzzle

**Файл:** `lib/entities/puzzle.dart`

Иммутабельное представление одного уровня-головоломки.

| Поле        | Тип                  | Описание                                          |
|-------------|----------------------|---------------------------------------------------|
| `id`        | `String`             | Уникальный идентификатор (например, `easy_1`)     |
| `difficulty`| `String`             | Сложность: `'easy'` или `'hard'`                  |
| `width`     | `int`                | Количество столбцов                               |
| `height`    | `int`                | Количество строк                                  |
| `solution`  | `List<List<int>>`    | Матрица [height × width]: 1 = закрашено, 0 = пусто|
| `rowHints`  | `List<List<int>>`    | Подсказки строк, генерируются из `solution`       |
| `colHints`  | `List<List<int>>`    | Подсказки столбцов, генерируются из `solution`    |
| `unlockRule`| `String?`            | Опциональное правило разблокировки                |

**Важно:** `rowHints` и `colHints` **не хранятся** в JSON-файлах и не в БД. Они вычисляются при загрузке через `PuzzleEngine.buildFromJson()`.

---

## GameState

**Файл:** `lib/entities/game_state.dart`

Иммутабельный снимок активной игровой сессии. Создаётся `GameNotifier.startGame()` и обновляется при каждом ходе через `copyWith`.

| Поле         | Тип                        | Описание                                     |
|--------------|----------------------------|----------------------------------------------|
| `puzzle`     | `Puzzle`                   | Текущая головоломка                          |
| `playerGrid` | `List<List<CellState>>`    | Матрица [height × width] с состоянием ячеек  |
| `mode`       | `GameMode`                 | Текущий режим: fill или cross                |
| `isSolved`   | `bool`                     | Флаг победы (по умолчанию false)             |

`playerGrid` инициализируется пустыми ячейками (`CellState.empty`). Победа определяется сравнением `filled`-ячеек с `puzzle.solution`.

---

## Progress

**Файл:** `lib/entities/progress.dart`

Запись о пройденном уровне. Наличие записи для `levelId` означает, что уровень решён.

| Поле          | Тип        | Описание                          |
|---------------|------------|-----------------------------------|
| `levelId`     | `String`   | ID пройденного уровня             |
| `completedAt` | `DateTime` | Время прохождения                 |

**Не хранить** производные данные (например, сколько попыток, время игры). Только факт прохождения.

---

## CellState

**Файл:** `lib/entities/cell_state.dart`

```dart
enum CellState { empty, filled, cross }
```

- `empty` — не тронута игроком
- `filled` — закрашена (игрок считает, что здесь должна быть клетка)
- `cross` — отмечена крестом (игрок уверен, что клетка пустая)

---

## GameMode

**Файл:** `lib/entities/game_mode.dart`

```dart
enum GameMode { fill, cross }
```

Переключается кнопкой в тулбаре. Определяет, какое действие происходит при нажатии на ячейку.
